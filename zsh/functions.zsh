# This file contains all autoloaded functions in the zfunc directory

# Completions 
autoload -Uz _ut
autoload -Uz _zi

autoload -Uz fgco
autoload -Uz fgl
autoload -Uz ut

#Stuff for prompt
autoload -Uz add-zsh-hook vcs_info
